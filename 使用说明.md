# 使用说明（给团队成员）

这份说明写给需要跑起这个项目的同学：从拿到仓库到看到大屏页面，照着做即可，全程约 10 分钟。

## 1. 项目是什么

停车楼智能管理系统 **后端**（Python Flask）。启动后会同时提供：

- **后端 API 接口**（`/api/...`，端口 5000）
- **前端大屏页面**（由后端直接托管，无需单独启动前端）

开发环境数据库是 SQLite 单文件（`instance/parking.db`，不进仓库），**执行 `init_data.py` 即可生成演示数据**（420 个车位、30 辆车、1000+ 设备等）。生产环境用在线 PostgreSQL（见 [docs/DEPLOY_RENDER.md](docs/DEPLOY_RENDER.md)）。

## 2. 准备环境

只需要 Python 3.8 及以上。验证：

```powershell
python --version
```

> 若提示 `python` 不是可识别的命令，重装 Python 时勾选 **"Add Python to PATH"**。

## 3. 拿到代码

方式一（推荐，装了 Git）：

```powershell
git clone <仓库地址>
cd parking_system
```

方式二：在 GitHub 仓库页面点 **Code → Download ZIP**，解压后进入 `parking_system` 文件夹。

## 4. 安装依赖

```powershell
# 创建虚拟环境（只需一次）
python -m venv venv

# 激活虚拟环境
venv\Scripts\Activate.ps1

# 安装依赖（国内网络建议加清华镜像）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> PowerShell 若报错"禁止运行脚本"，先执行：
> `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

## 5. 建表并启动后端

```powershell
# 建表（Flask-Migrate 迁移，首次运行执行一次）
set FLASK_APP=app.py
flask db upgrade

# （可选）填充演示数据（幂等，可重复执行）
python init_data.py

# 启动后端
python app.py
```

看到 `Running on http://0.0.0.0:5000` 即成功（开发模式以 debug 启动，改代码自动重载）。

## 6. 访问页面

浏览器打开：

| 页面 | 地址 |
| --- | --- |
| 前端大屏 | <http://127.0.0.1:5000/> |
| 测试面板 | <http://127.0.0.1:5000/test.html> |
| 健康检查 | <http://127.0.0.1:5000/api/hello> |

## 7. 数据库说明

- 开发数据库文件：`instance/parking.db`（**不进仓库**，克隆后需自己初始化）
- **首次初始化**：`flask db upgrade` 建表 → `python init_data.py` 填充演示数据（幂等，已有数据自动跳过）
- **重置演示数据**：`python init_data.py --force`（清空后重新填充）
- **修改表结构**：改 `models.py` 后执行 `flask db migrate -m "说明"` 生成迁移文件，再 `flask db upgrade` 升级（不丢数据）
- **生产环境**：在线 PostgreSQL + 部署指南见 [docs/DEPLOY_RENDER.md](docs/DEPLOY_RENDER.md)；旧数据迁移用 `scripts/migrate_to_online.py`

## 8. 接口速查

| 接口 | 说明 |
| --- | --- |
| `/api/hello` | 健康检查 |
| `/api/spots` | 车位列表（`?floor=3` 按楼层过滤） |
| `/api/devices` | 设备列表（`?type=`、`?floor=` 过滤） |
| `/api/energy/24h` | 24 小时能耗记录 |
| `/api/events` | 告警/事件列表 |
| `/api/fire/emergency` | 消防应急处置总览（紧急事件/风险点/传感器异常/处置建议） |

## 9. 常见问题

### 9.1 端口 5000 被占用

```
OSError: [WinError 10048]
```

```powershell
netstat -ano | findstr :5000
taskkill /PID <查到的PID> /F
```

### 9.2 局域网队友访问不到

后端已监听 `0.0.0.0`，队友访问 `http://<你的电脑IP>:5000` 即可：

```powershell
ipconfig   # 找 IPv4 地址
```

仍不通就检查 Windows 防火墙是否放行 5000 端口。

### 9.3 启动报"数据库表不存在"

说明还没建表或数据库损坏，先 `flask db upgrade` 建表，再 `python init_data.py` 填充演示数据。

### 9.4 MQTT 相关报错

忽略即可。MQTT 客户端默认关闭，不影响任何功能。

## 10. 更多

- 详细安装教程：[docs/后端启动与依赖安装教程.md](docs/后端启动与依赖安装教程.md)
- 打包 exe 版说明：[使用说明.txt](使用说明.txt)（exe 不在仓库中，需自行用 PyInstaller 打包）
